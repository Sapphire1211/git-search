{
  "name": "git-search",
  "private": true,
  "version": "0.0.0",
  "description": "A web application to search github projects",
  "repository": "",
  "license": "MIT",
  "dependencies": {
    "angular": "^1.7.5",
    "angular-route": "^1.7.5",
    "angular-resource": "^1.7.5"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "protractor": "^5.4.1"
  },
  "scripts": {
    "update-deps": "npm update",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1 ./app",
    "pretest": "npm install",
    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update --gecko false",
    "preprotractor": "npm run update-webdriver",
    "protractor": "protractor tests/conf.js"
  }
}
